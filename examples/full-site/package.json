{
  "name": "astro-blog-cms-example",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "wrangler pages dev ./dist",
    "deploy": "astro build && wrangler pages deploy ./dist",
    "db:migrate": "wrangler d1 execute my-blog-db --file=../../migrations/0001_blog_schema.sql && wrangler d1 execute my-blog-db --file=../../migrations/0002_blog_engagement.sql && wrangler d1 execute my-blog-db --file=../../migrations/0003_settings_table.sql",
    "setup": "node scripts/setup.js"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^12.0.0",
    "@dylanburkey/astro-blog-cms": "workspace:*",
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.99.0"
  }
}
